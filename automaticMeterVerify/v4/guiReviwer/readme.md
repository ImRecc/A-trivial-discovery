GuiReviewer/
│
├── main.py              # Your main Python logic
├── audit_ui.ui          # The Qt Designer file (for others to edit)
├── ui_audit.py          # The compiled UI python file
├── requirements.txt     # Generated by pipreqs
├── README.md            # The documentation
└── dist/
    └── AuditSystem.exe  # packed executable 

#### 如果要自己编译：
`pip install pipreqs | pipreqs . --encoding=utf8 | pip install -r requirements.txt`
来安装依赖

GUI Framework: PySide6 (Qt for Python). \
Data I/O: openpyxl and pandas. Used for reading target values and writing audit results/cell background colors back to the .xlsx file.\
Packaging: PyInstaller. Used to compile the Python script and UI into a standalone .exe.\
